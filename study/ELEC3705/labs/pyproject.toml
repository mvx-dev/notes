[project]
name = "labs"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Oskar Meszaros", email = "oskardmeszaros@protonmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "jupyter>=1.1.1",
]

[project.scripts]
labs = "labs:main"

[build-system]
requires = ["uv_build>=0.12.17,<0.13.0"]
build-backend = "uv_build"
